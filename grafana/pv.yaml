apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana
  namespace: infra
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 20Gi
  claimRef:
    name: grafana
    namespace: infra
  hostPath:
    path: /mnt/data/grafana
    